# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: iOS-ipa-build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: 🎉 iOS Build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64
      - run: flutter pub get
      

      - run: pod repo update
        working-directory: ios

      - run: flutter build ios --release

      - run: mkdir Payload
        working-directory: build/ios/iphoneos

      - run: mv Runner.app/ Payload
        working-directory: build/ios/iphoneos

      - name: Zip output
        run: zip -qq -r -9 FlutterIpaExport.ipa Payload
        working-directory: build/ios/iphoneos

      - name: Upload binaries to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: build/ios/iphoneos/FlutterIpaExport.ipa
          tag: v1.0
          overwrite: true
          body: "This is first release"

# name: Deploy to Firebase Hosting on merge
# 'on':
#   push:
#     branches:
#       - master
# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - uses: actions/setup-java@v1
#         with:
#           java-version: '12.x'
#       - uses: subosito/flutter-action@v1
#       - name: 'Run flutter pub get'
#         run: flutter pub get
#       - name: 'Build Web App'
#         run: flutter build web
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
#           channelId: live
#           projectId: showcase-312b6
#         env:
#           FIREBASE_CLI_PREVIEWS: hostingchannels
  



# jobs:
#   build:
#     runs-on: macos-latest  # Ensure you are using the latest macOS runner
    
#     steps:
#       - uses: actions/checkout@v2
#       - name: Set up Xcode (use the default Xcode version)
#         run: |
#           # Print available Xcode paths and select the correct one
#           sudo xcode-select -s /Applications/Xcode.app
#           xcodebuild -version  # Confirm the version used
        
#       - name: Build iOS Ap
#         run: |
#           # Your build steps
#           xcodebuild -scheme MyApp -sdk iphoneos -configuration Release clean build
#           xcodebuild -exportArchive -archivePath ./build/MyApp.xcarchive -exportOptionsPlist exportOptions.plist -exportPath ./build/ipa
